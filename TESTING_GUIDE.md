# Руководство по тестированию KG Embeddings

## После обучения модели

После выполнения:
```bash
python create_kg_embeddings.py --epochs 150 --dim 64
```

## Шаг 1: Быстрая проверка загрузки

Проверьте, что embeddings загружаются без ошибок:

```bash
python test_kg_embeddings.py
```

Этот скрипт проверит:
- ✅ Загрузку embeddings
- ✅ Качество предсказаний (score правильных vs неправильных связей)
- ✅ Примеры поиска
- ✅ Поиск похожих сущностей

## Шаг 2: Проверка качества

### Что смотреть в выводе:

1. **Средний score правильных связей:**
   - > -5.0 → 🎯 Отличное качество
   - > -10.0 → 👍 Хорошее качество
   - < -10.0 → ⚠️ Среднее качество (может потребоваться больше эпох)

2. **Разница между правильными и неправильными:**
   - Должна быть очевидна (правильные ближе к 0)
   - Если разница маленькая → модель плохо обучена

3. **Примеры поиска:**
   - Должны находиться правильные автомобили от производителя
   - Score должен быть разумным (> -10.0)

## Шаг 3: Интерактивный поиск

Если тесты прошли успешно, попробуйте интерактивный поиск:

```bash
python search_with_kg_embeddings.py
```

Это покажет примеры:
- Поиск автомобилей от производителя
- Поиск похожих сущностей

## Если результаты плохие

### Признаки плохого обучения:

❌ Score правильных связей < -15.0
❌ Разница между правильными и неправильными < 2.0
❌ Поиск возвращает неправильные результаты

### Решения:

1. **Увеличить количество эпох:**
   ```bash
   python create_kg_embeddings.py --epochs 200 --dim 64
   ```

2. **Увеличить размерность:**
   ```bash
   python create_kg_embeddings.py --epochs 150 --dim 128
   ```

3. **Уменьшить learning rate:**
   (Нужно изменить в коде `create_kg_embeddings.py`)

## Ожидаемые результаты

После 150 эпох обучения вы должны увидеть:

```
✅ Результаты:
   Средний score ПРАВИЛЬНЫХ связей: -3.5
   Средний score НЕПРАВИЛЬНЫХ связей: -12.8
   Разница: 9.3
   ✅ Модель правильно различает связи!
   🎯 Отличное качество (score правильных > -5.0)
```

## Быстрая проверка одной командой

```bash
# Обучение + тестирование
python create_kg_embeddings.py --epochs 150 --dim 64 && python test_kg_embeddings.py
```

## Что дальше?

После успешного тестирования:

1. ✅ Используйте `search_with_kg_embeddings.py` для поиска
2. ✅ Интегрируйте в свои приложения
3. ✅ Экспериментируйте с разными запросами

